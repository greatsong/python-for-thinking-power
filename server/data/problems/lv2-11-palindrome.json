{
  "id": "lv2-11-palindrome",
  "title": "앞뒤가 같은 단어",
  "difficulty": 2,
  "category": "string",
  "description": "## 🔄 회문(팰린드롬)을 찾아라!\n\n앞에서 읽어도 뒤에서 읽어도 똑같은 단어를 **회문**이라고 해요.\n예: `level`, `racecar`, `기러기`, `토마토`\n\n파이썬의 **슬라이싱**으로 문자열을 뒤집을 수 있어요!\n\n```python\nword = input()\nif word == word[::-1]:\n    print(\"회문이에요!\")\nelse:\n    print(\"회문이 아니에요\")\n```\n\n`word[::-1]` → 문자열을 거꾸로!\n\n---\n\n## 문제\n\n단어를 입력받아 회문이면 `회문이에요!`, 아니면 `회문이 아니에요`를 출력하세요.\n\n### 예시\n```\n입력: racecar\n출력: 회문이에요!\n\n입력: hello\n출력: 회문이 아니에요\n```",
  "starter_code": "word = input()\n\n# 회문인지 확인하세요\n",
  "test_cases": [
    { "input": "racecar", "expected_output": "회문이에요!", "description": "racecar는 회문" },
    { "input": "hello", "expected_output": "회문이 아니에요", "description": "hello는 회문 아님" },
    { "input": "기러기", "expected_output": "회문이에요!", "description": "기러기는 한글 회문" },
    { "input": "level", "expected_output": "회문이에요!", "description": "level은 회문" },
    { "input": "python", "expected_output": "회문이 아니에요", "description": "python은 회문 아님" }
  ],
  "hints": [
    "word[::-1] 은 문자열을 뒤집어요. 슬라이싱의 세 번째 인자가 -1이면 역순!",
    "word == word[::-1] 이면 회문이에요."
  ],
  "expected_approaches": [
    { "tag": "슬라이싱 역순", "description": "word == word[::-1]" },
    { "tag": "reversed() 활용", "description": "word == \"\".join(reversed(word))" }
  ],
  "explanation": "## 풀이 해설\n\n```python\nword = input()\nif word == word[::-1]:\n    print(\"회문이에요!\")\nelse:\n    print(\"회문이 아니에요\")\n```\n\n`word[::-1]`은 슬라이싱 `[시작:끝:스텝]`에서 스텝을 `-1`로 설정한 것!\n처음부터 끝까지, 거꾸로 진행하므로 문자열이 뒤집혀요."
}
