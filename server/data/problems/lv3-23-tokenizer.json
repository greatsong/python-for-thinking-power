{
  "id": "lv3-23-tokenizer",
  "title": "수식 토크나이저",
  "difficulty": 3,
  "category": "string",
  "description": "## 🔬 수식을 분해해봐요!\n\n`3+12*5-8` 같은 수식을 숫자와 연산자로 분리하는 프로그램이에요.\n\n**토큰화(Tokenizing)**는 텍스트를 의미 있는 단위로 나누는 것이에요.\n\n```\n입력: 3+12*5-8\n출력:\n숫자: [3, 12, 5, 8]\n연산자: [+, *, -]\n```\n\n### 접근 방법\n숫자가 연속으로 나올 수 있어요. 한 글자씩 읽으면서 숫자를 누적해요.\n\n---\n\n## 문제\n\n사칙연산 수식을 입력받아 숫자 목록과 연산자 목록을 각각 출력하세요.\n\n### 예시\n```\n입력: 3+12*5-8\n출력:\n숫자: [3, 12, 5, 8]\n연산자: [+, *, -]\n```",
  "starter_code": "expr = input()\n\n# 수식에서 숫자와 연산자를 분리하세요\n",
  "test_cases": [
    { "input": "3+12*5-8", "expected_output": "숫자: [3, 12, 5, 8]\n연산자: [+, *, -]", "description": "기본 수식" },
    { "input": "100+200", "expected_output": "숫자: [100, 200]\n연산자: [+]", "description": "두 자리 이상 숫자" },
    { "input": "5*5", "expected_output": "숫자: [5, 5]\n연산자: [*]", "description": "단순 곱셈" }
  ],
  "hints": [
    "current_num = \"\" 로 현재 숫자를 문자열로 누적하세요.",
    "연산자(+,-,*,/)를 만나면: numbers.append(int(current_num)); current_num = \"\"",
    "반복이 끝나면 마지막 숫자도 추가하는 것 잊지 마세요!"
  ],
  "expected_approaches": [
    { "tag": "문자 단위 파싱", "description": "current_num 누적, 연산자 만나면 저장 후 초기화" },
    { "tag": "re 모듈 활용", "description": "re.findall(r'\\d+', expr)로 숫자, re.findall(r'[+\\-*/]', expr)로 연산자" }
  ],
  "explanation": "## 풀이 해설\n\n```python\nexpr = input()\nnumbers = []\noperators = []\ncurrent_num = \"\"\n\nfor c in expr:\n    if c.isdigit():\n        current_num += c  # 숫자 문자를 누적\n    else:\n        numbers.append(int(current_num))  # 숫자 저장\n        operators.append(c)               # 연산자 저장\n        current_num = \"\"                  # 초기화\n\nnumbers.append(int(current_num))  # ⚠️ 마지막 숫자!\n\nprint(f\"숫자: {numbers}\")\nprint(f\"연산자: {operators}\")\n```\n\n정규식 버전 (간단!):\n```python\nimport re\nnumbers = list(map(int, re.findall(r'\\d+', expr)))\noperators = re.findall(r'[+\\-*/]', expr)\n```\n\n**토크나이저**는 컴파일러, 인터프리터, 자연어 처리에서 핵심 개념이에요!\n파이썬도 코드를 실행하기 전에 이런 과정을 거쳐요."
}
