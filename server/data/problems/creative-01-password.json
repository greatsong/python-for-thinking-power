{
  "id": "creative-01-password",
  "title": "비밀번호 해독기",
  "difficulty": 4,
  "category": "string",
  "description": "## 비밀번호 해독기\n\n여러분은 천재 해커입니다! 🕵️ 적의 비밀 메시지를 가로챘는데, **시저 암호(Caesar Cipher)**로 암호화되어 있습니다.\n\n시저 암호란? 알파벳을 일정한 칸수만큼 밀어서 암호화하는 방식입니다.\n\n```\n예시: shift = 3\na → d, b → e, c → f, ..., x → a, y → b, z → c\n```\n\n### 특별한 규칙\n\n이 암호에는 특별한 규칙이 있어요:\n- 암호문의 **첫 번째 문자**에 이동 칸수(shift)가 숨겨져 있습니다\n- 첫 번째 문자의 알파벳 순서(a=1, b=2, ..., z=26)가 바로 shift 값입니다\n- 첫 번째 문자를 제외한 나머지를 해당 shift만큼 **뒤로 되돌리면** 원래 메시지가 됩니다\n- 소문자 알파벳만 사용하며, 알파벳이 아닌 문자(공백, 숫자 등)는 변환하지 않습니다\n\n```mermaid\nflowchart LR\n    A[암호문 첫 글자] -->|알파벳 순서 = shift| B[shift 값 결정]\n    C[나머지 암호문] -->|각 글자 - shift| D[원래 메시지]\n    B --> D\n```\n\n### 해독 과정 예시\n\n```\n암호문: \"chgf\"\n\n1단계: 첫 글자 'c' → c는 3번째 알파벳 → shift = 3\n2단계: 나머지 \"hgf\"를 shift=3만큼 뒤로\n  h(8) - 3 = e(5)\n  g(7) - 3 = d(4)\n  f(6) - 3 = c(3)\n3단계: 원래 메시지 = \"edc\"\n```\n\n### 입력\n- 한 줄의 암호화된 문자열 (첫 글자는 shift를 나타내는 소문자 알파벳)\n\n### 출력\n- 해독된 문자열 (첫 글자 제외, 나머지를 shift만큼 되돌린 결과)\n\n### 예시\n\n**예시 1:**\n```\n입력:\nchgf\n\n출력:\nedc\n```\n> 첫 글자 'c' → shift=3, \"hgf\"를 3칸 뒤로 → \"edc\"\n\n**예시 2:**\n```\n입력:\nabcde\n\n출력:\nabcd\n```\n> 첫 글자 'a' → shift=1, \"bcde\"를 1칸 뒤로 → \"abcd\"\n\n**예시 3:**\n```\n입력:\nckhoor zruog\n\n출력:\nhello world\n```\n> 첫 글자 'c' → shift=3, \"khoor zruog\"를 3칸 뒤로 → \"hello world\" (공백은 그대로)",
  "starter_code": "cipher = input()\n\n# 첫 글자에서 shift 값을 추출하세요\n# 나머지 글자를 shift만큼 뒤로 되돌리세요\n# 알파벳이 아닌 문자는 그대로 유지!\n",
  "test_cases": [
    { "input": "chgf", "expected_output": "edc", "description": "기본 테스트 (shift=3)" },
    { "input": "abcde", "expected_output": "abcd", "description": "shift=1 테스트" },
    { "input": "ckhoor zruog", "expected_output": "hello world", "description": "공백 포함 (shift=3)" },
    { "input": "bqnf", "expected_output": "old", "description": "shift=2 테스트" },
    { "input": "abn", "expected_output": "am", "description": "shift=1, 짧은 문자열" }
  ],
  "hints": [
    "첫 글자의 shift 값은 ord(첫글자) - ord('a') + 1로 구할 수 있어요.",
    "알파벳을 되돌릴 때 a보다 작아지면? 26을 더해서 z 쪽으로 돌아가야 해요! (모듈러 연산)",
    "chr()와 ord()를 활용하세요. 변환 공식: chr((ord(c) - ord('a') - shift) % 26 + ord('a'))"
  ],
  "expected_approaches": [
    { "tag": "ord/chr + 모듈러 연산", "description": "각 문자를 숫자로 변환하고 shift를 빼서 모듈러 26 적용" },
    { "tag": "알파벳 문자열 인덱싱", "description": "'abcdefghijklmnopqrstuvwxyz' 문자열에서 인덱스를 찾아 이동" },
    { "tag": "브루트포스", "description": "가능한 모든 shift(1~26)를 시도하며 의미 있는 메시지를 찾기" }
  ],
  "explanation": "## 풀이 해설\n\n### 핵심 개념\n이 문제는 **시저 암호(Caesar Cipher)** 해독이야! 핵심은 두 가지야:\n1. 암호문의 **첫 글자**에서 shift 값을 뽑아내기 (a=1, b=2, ..., z=26)\n2. 나머지 글자들을 shift만큼 **뒤로 되돌리기** (모듈러 연산 사용)\n\n`ord()`와 `chr()` 함수가 핵심 도구야. `ord('a')` = 97, `chr(97)` = 'a' 이런 식으로 글자와 숫자를 변환할 수 있어!\n\n```mermaid\nflowchart TD\n    A[\"암호문 입력\"] --> B[\"첫 글자 추출\"]\n    B --> C[\"shift = ord(첫글자) - ord('a') + 1\"]\n    A --> D[\"나머지 글자들\"]\n    D --> E{\"각 글자가 알파벳?\"}\n    E -->|Yes| F[\"chr((ord(c) - ord('a') - shift) % 26 + ord('a'))\"]\n    E -->|No| G[\"그대로 유지\"]\n    F --> H[\"결과 문자열에 추가\"]\n    G --> H\n    H --> I[\"해독된 메시지 출력\"]\n    style C fill:#dbeafe,stroke:#3b82f6,color:#1e3a5f,stroke-width:2px\n    style F fill:#dcfce7,stroke:#22c55e,color:#14532d,stroke-width:2px\n```\n\n### 대표 풀이: ord/chr + 모듈러 연산\n```python\ncipher = input()\nshift = ord(cipher[0]) - ord('a') + 1\nresult = ''\nfor c in cipher[1:]:\n    if c.isalpha():\n        result += chr((ord(c) - ord('a') - shift) % 26 + ord('a'))\n    else:\n        result += c\nprint(result)\n```\n\n**동작 확인:**\n- 테스트 1: `chgf` → shift=3, `hgf` → h(7-3=4→e), g(6-3=3→d), f(5-3=2→c) → `edc` ✓\n- 테스트 2: `abcde` → shift=1, `bcde` → b(1-1=0→a), c(2-1=1→b), d(3-1=2→c), e(4-1=3→d) → `abcd` ✓\n- 테스트 3: `ckhoor zruog` → shift=3, `khoor zruog` → `hello world` ✓\n- 테스트 4: `bqnf` → shift=2, `qnf` → q(16-2=14→o), n(13-2=11→l), f(5-2=3→d) → `old` ✓\n- 테스트 5: `abn` → shift=1, `bn` → b(1-1=0→a), n(13-1=12→m) → `am` ✓\n\n### 다른 풀이 방법\n\n**알파벳 문자열 인덱싱**\n숫자 계산 대신 알파벳 문자열에서 위치를 찾아 이동하는 방법이야.\n```python\ncipher = input()\nalpha = 'abcdefghijklmnopqrstuvwxyz'\nshift = alpha.index(cipher[0]) + 1\nresult = ''\nfor c in cipher[1:]:\n    if c in alpha:\n        idx = (alpha.index(c) - shift) % 26\n        result += alpha[idx]\n    else:\n        result += c\nprint(result)\n```\n`alpha.index(c)`로 알파벳에서의 위치를 찾고, shift만큼 빼서 새 위치의 글자를 가져와. `% 26`이 원형으로 돌아가는 걸 처리해줘!\n\n**리스트 컴프리헨션 (한 줄 풀이)**\n파이썬답게 한 줄로 압축할 수도 있어.\n```python\ncipher = input()\nshift = ord(cipher[0]) - ord('a') + 1\nprint(''.join(chr((ord(c)-ord('a')-shift)%26+ord('a')) if c.isalpha() else c for c in cipher[1:]))\n```\n\n### 주의할 점\n- **모듈러 연산(`% 26`)이 핵심이야!** shift를 빼면 음수가 될 수 있는데, 파이썬의 `%` 연산은 음수도 올바르게 처리해줘. 예: `(-3) % 26 = 23`\n- 첫 글자는 shift 값을 나타내는 거라 **출력에 포함하면 안 돼!** `cipher[1:]`부터 처리해야 해.\n- **공백, 숫자 등 알파벳이 아닌 문자**는 변환하지 않고 그대로 둬야 해. `isalpha()`로 체크하면 돼.\n- shift 값은 `ord(c) - ord('a') + 1`이야. `+1`을 빼먹으면 'a'가 shift=0이 되어버려!"
}
