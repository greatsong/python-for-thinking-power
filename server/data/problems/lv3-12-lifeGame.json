{
  "id": "lv3-12-lifeGame",
  "title": "생명 게임 1세대",
  "difficulty": 3,
  "category": "algorithm",
  "description": "## 🦠 콘웨이의 생명 게임!\n\n1970년대에 만들어진 유명한 규칙이에요. 간단한 규칙에서 복잡한 패턴이 나와요!\n\n**규칙 (1차원 단순화 버전)**:\n- `#` = 살아있는 세포, `.` = 죽은 세포\n- 각 칸의 **양 옆 이웃** 중 살아있는 수를 세요\n- **다음 세대 규칙**: 살아있는 이웃이 정확히 1명이면 `#`, 그 외에는 `.`\n  - 이웃 0명 → `.` (외로워서 죽음/탄생 안 함)\n  - 이웃 1명 → `#` (적절한 환경! 생존 또는 탄생)\n  - 이웃 2명 → `.` (과밀로 죽음/탄생 안 함)\n\n> 양 끝은 이웃이 없는 쪽은 `.`으로 처리해요.\n\n---\n\n## 문제\n\n세포 상태 문자열을 입력받아 1세대 후 상태를 출력하세요.\n\n### 예시\n```\n입력: .###.\n출력: ##.##\n```\n\n> 설명:\n> - 위치0 `.`: 오른쪽 `#` 1개 → 이웃 1명 → `#`\n> - 위치1 `#`: 왼쪽 `.`, 오른쪽 `#` → 이웃 1명 → `#`\n> - 위치2 `#`: 왼쪽 `#`, 오른쪽 `#` → 이웃 2명 → `.`\n> - 위치3 `#`: 왼쪽 `#`, 오른쪽 `.` → 이웃 1명 → `#`\n> - 위치4 `.`: 왼쪽 `#` 1개 → 이웃 1명 → `#`",
  "starter_code": "cells = input()\nn = len(cells)\n\n# 1세대 후 상태를 계산하세요\n",
  "test_cases": [
    { "input": ".###.", "expected_output": "##.##", "description": "양쪽 끝 탄생, 가운데 과밀 사망" },
    { "input": "#####", "expected_output": "#...#", "description": "양 끝만 생존 (이웃 1명)" },
    { "input": ".....", "expected_output": ".....", "description": "모두 죽은 경우" },
    { "input": ".#.", "expected_output": "#.#", "description": "양쪽에 탄생, 가운데 외로워 사망" }
  ],
  "hints": [
    "각 위치 i에 대해 왼쪽(i-1)과 오른쪽(i+1)의 # 개수를 세요.",
    "경계 처리: i=0이면 왼쪽 없음, i=n-1이면 오른쪽 없음",
    "neighbor_count = (cells[i-1]=='#' if i>0 else 0) + (cells[i+1]=='#' if i<n-1 else 0)"
  ],
  "expected_approaches": [
    { "tag": "직접 이웃 세기 O(N)", "description": "각 위치에서 if문으로 왼쪽/오른쪽 경계 확인 후 이웃 수 카운트" },
    { "tag": "패딩(양 끝에 . 추가) O(N)", "description": "'.'+cells+'.'로 감싸서 경계 조건 제거, 항상 [i-1]과 [i+1] 접근 가능" },
    { "tag": "리스트 컴프리헨션 + 슬라이싱 O(N)", "description": "cells를 숫자 배열로 변환 후 left[:-2]+right[2:]로 이웃 합 벡터 계산" }
  ],
  "explanation": "## 풀이 해설\n\n**문제 분해**:\n1. 각 위치마다 이웃 수 계산\n2. 이웃 수에 따라 다음 상태 결정\n3. 결과 합치기\n\n```python\ncells = input()\nn = len(cells)\nnext_gen = []\n\nfor i in range(n):\n    # 이웃 수 계산 (경계 처리 포함)\n    neighbors = 0\n    if i > 0 and cells[i-1] == '#':     # 왼쪽\n        neighbors += 1\n    if i < n-1 and cells[i+1] == '#':   # 오른쪽\n        neighbors += 1\n    \n    # 규칙 적용: 이웃 1명만 살아남음\n    if neighbors == 1:\n        next_gen.append('#')\n    else:\n        next_gen.append('.')\n\nprint(''.join(next_gen))\n```\n\n**경계 조건**이 이 문제의 핵심! 배열 밖을 참조하지 않도록 항상 범위를 확인해요."
}
